---
title: "Beyond Spatial Compression: Interface-Centric Generative States for Open-World 3D Structure"
title_zh: 超越空间压缩：面向开放世界三维结构的以界面为中心的生成状态
authors: "Xiang Chen, Alexander Binder"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10438v1"
tags: ["query:part-aware"]
score: 9.0
evidence: 表示部件级三维结构及附着关系
tldr: 当前3D tokenizer主要将表示视作空间压缩，导致组件归属、附着有效性等语义信息隐式编码在潜在流中，解码时无法独立寻址。本文提出一种接口中心生成状态范式：token化构建的是可查询、约束与修复的操作状态，而非被动压缩码。实例方法C2LT-3D将表示因子化为典型局部几何、分区条件上下文和关系接缝变量，针对性解决姿态泄漏、跨组件干扰和无效附着问题。在开放世界多组件资产上的零样本评估中，结构鲁棒性大幅提升，且潜在变量在对抗附着设置下保持可操作，验证了离散状态用于组装级结构推理的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10438v1/fig-001.webp\", \"caption\": \"\", \"page\": 6, \"index\": 1, \"width\": 5877, \"height\": 1110}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-002.webp\", \"caption\": \"\", \"page\": 8, \"index\": 2, \"width\": 1322, \"height\": 896}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-003.webp\", \"caption\": \"\", \"page\": 9, \"index\": 3, \"width\": 3360, \"height\": 908}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-004.webp\", \"caption\": \"\", \"page\": 11, \"index\": 4, \"width\": 1542, \"height\": 622}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-005.webp\", \"caption\": \"\", \"page\": 15, \"index\": 5, \"width\": 1796, \"height\": 1456}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-006.webp\", \"caption\": \"\", \"page\": 17, \"index\": 6, \"width\": 1322, \"height\": 1976}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-007.webp\", \"caption\": \"\", \"page\": 21, \"index\": 7, \"width\": 1760, \"height\": 2120}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-008.webp\", \"caption\": \"\", \"page\": 25, \"index\": 8, \"width\": 3149, \"height\": 1741}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-009.webp\", \"caption\": \"\", \"page\": 25, \"index\": 9, \"width\": 5529, \"height\": 1075}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-010.webp\", \"caption\": \"\", \"page\": 26, \"index\": 10, \"width\": 2800, \"height\": 1306}, {\"url\": \"assets/figures/arxiv/2605.10438v1/fig-011.webp\", \"caption\": \"\", \"page\": 27, \"index\": 11, \"width\": 3809, \"height\": 1037}]"
motivation: 现有3D tokenizer将表示简化为空间压缩，隐式编码组件归属与附着关系，在开放世界多组件资产中引发表示不匹配。
method: 提出接口中心生成状态，将token构建为可操作状态；设计C2LT-3D，因子化为典型局部几何、分区上下文和关系接缝变量。
result: 零样本评估显示结构鲁棒性显著提升，潜在变量在对抗附着设置下仍保持可操作，支持附着验证与修复。
conclusion: 开放世界3D生成表示须评估离散状态是否可用于组装级结构推理，而不仅是重建保真度。
---

## 摘要
当前的三维分词器在很大程度上将表示视为空间压缩：紧凑的代码重建表面几何，但使组件归属和附件有效性保持隐含。在具有相交组件、嘈杂拓扑和弱规范结构的开放世界资产中，这造成了表示不匹配：局部形状、组件身份和装配关系在潜在流中纠缠在一起，在解码期间无法原生地寻址。我们提出了一种替代观点，即以界面为中心的生成状态，其中分词器构建一个操作状态而不是被动压缩代码。该状态将局部几何、组件归属和附件有效性公开为可以在解码期间查询、约束和修复的变量。我们用组件条件的规范局部令牌（C2LT-3D）实例化该公式，将表示分解为规范局部几何、分区条件上下文和关系接缝变量。每个因子针对以压缩为中心的令牌的不同故障模式：姿态泄漏、跨组件干扰或无效的局部附件。这种公开状态支持附件验证、潜在结构修复、有针对性的干预以及受约束的序列化，而无需单独的事后结构恢复模块。在单一对象CAD模型上训练，并在开放世界多组件资产上进行零样本评估后，C2LT-3D提高了结构鲁棒性，并表明其潜在变量在对抗性附件设置下仍然可操作。这些结果表明，开放世界三维生成表示不仅应通过重建保真度来评估，还应通过其离散状态是否对于装配级结构推理保持可操作来评估。

## Abstract
Current 3D tokenizers largely treat representation as spatial compression: compact codes reconstruct surface geometry, but leave component ownership and attachment validity implicit. In open-world assets with intersecting components, noisy topology, and weak canonical structure, this creates a representation mismatch: local shape, component identity, and assembly relations become entangled in a latent stream and are not natively addressable during decoding. We formulate an alternative view, interface-centric generative states, in which tokenization constructs an operational state rather than a passive compressed code. The state exposes local geometry, component ownership, and attachment validity as variables that can be queried, constrained, and repaired during decoding. We instantiate this formulation with Component-Conditioned Canonical Local Tokens (C2LT-3D), factorizing representation into canonical local geometry, partition-conditioned context, and relational seam variables. Each factor targets a distinct failure mode of compression-centric tokens: pose leakage, cross-component interference, or invalid local attachment. This exposed state supports attachment validation, latent structural repair, targeted intervention, and constrained serialization without a separate post-hoc structure recovery module. Trained on single-object CAD models and evaluated zero-shot on open-world multi-component assets, C2LT-3D improves structural robustness and shows that its latent variables remain actionable under adversarial attachment settings. These results suggest that open-world 3D generative representations should be evaluated not only by reconstruction fidelity, but by whether their discrete states remain operational for assembly-level structural reasoning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：当前主流的3D tokenizer（如MeshGPT、BPT等）将三维表示视为纯粹的空间压缩任务，生成的紧凑代码能重建表面几何，但**组件归属（ownership）、附着有效性（attachment validity）和装配关系等语义结构信息被隐含在潜在码流中**，无法在解码时被独立寻址和操作。
- **研究动机**：在面向“开放世界”的3D资产（如Objaverse数据集中包含大量相交组件、非流形内部、弱规范姿态的物体）时，这种“压缩中心”的表示导致**表示不匹配**——局部形状、部件身份和组合关系纠缠在一起，引发跨组件泄漏、无效接缝和结构错误。
- **整体含义**：论文提出了一种范式转变，即从被动压缩码转向**以界面为中心的生成状态（Interface-Centric Generative States）**。这种生成表示的生命周期中，token化应当构建一个**可查询、可约束、可修复的操作状态**，而不仅仅是用于重建的压缩代码。最终目标是使3D生成模型不仅关注几何保真度，还要能支持装配层面的结构推理。

### 2. 论文提出的方法论

- **核心思想**：将表示因子化为三类显式变量，使“局部形状”、“组件归属”和“装配关系”在解码过程中始终可寻址（addressable），形成离散关系图\( G_{\text{C2LT}} \)。
- **关键技术实例——C2LT-3D**：
    - **典型局部几何（Canonical Local Geometry）**：为每个表面图块（chart）构建确定性局部标架，通过部分典型化（partial canonicalization）去除主要姿态变化，使码本学习专注于形状而非姿态变体。公式为 \(\tilde{X}_i = s^{-1}_i R^\top_i (X_i - a_i)\)。
    - **分区条件上下文（Partition-Conditioned Context）**：利用无监督宏组件提示（如连通分量、几何分裂等）构建软结构偏置，通过分区敏感的注意力机制（partition-conditioned attention）在上下文Transformer中注入组件所有权信息，抑制近邻但不同部件的特征相互泄漏。注意力偏置公式为 \(\alpha_{ij} = \frac{\exp(q^\top_i k_j/\sqrt{d} + b_{\text{part}}(i,j) + b_{\text{geom}}(i,j))}{\sum_\ell \exp(\dots)}\)。
    - **关系接缝先验（Relational Seam Prior）**：作为装配操作器，对候选图块对预测兼容性分数 \(\hat{C}_{ij}\)、相对位姿精化 \(\Delta \hat{T}_{ij}\) 和碰撞风险 \(\hat{y}^{\text{coll}}_{ij}\)。这使模型能直接对附着关系进行评分、排序和修复。
- **操作流程**：生成过程被视为受约束的状态转移序列。新生成的图块需经过上下文化、附着验证，并可通过接缝操作器进行潜在修复。解码时使用一个**确定性的部件归属对象实现映射（Component-Owned Object Realization）**来抑制跨部件泄漏。

### 3. 实验设计

- **训练数据**：仅在**ShapeNet**的单体、水密CAD模型子集上训练（约40,000对象）。
- **评估数据与场景**：
    - **零样本开放世界评估**：在**Objaverse-LVIS**的几何清晰子集上进行，包含1,024个未见过、多组件、结构复杂的对象。
    - **潜空间修复基准**：从相同数据源构建了包含1,964个跨部件修复任务的边库，其中包含1,360个“困难”任务和1,194个“启发式失败”任务，专门测试局部几何误导时的附着推理能力。
    - **受控序列化**：使用一个轻量解码器评估离散状态的序列化生成质量。
- **基准与方法对比**：
    - **性能基线**：直接对比外部的压缩中心方法**BPT**和内部构建的纯空间压缩基线**VQ-Patch**。
    - **释放接口对照**：分析**MeshAnythingV2**等生成式模型的输出，虽然其几何表面可能合理，但其接口并不原生暴露组件归属、附着变量或可修复接缝状态，以此凸显“操作性”的缺失。
- **评估指标**：除标准几何指标（Chamfer、Hausdorff、法向一致性）外，专门设计了**结构鲁棒性指标**，包括部件分离度评分（Separation Score）、跨部件污染率（Contamination Rate），以及用于潜空间修复的Valid@1和Parent@1等。

### 4. 资源与算力

- **GPU型号**：所有计时测量均基于**单张NVIDIA GeForce RTX 5090**。
- **训练时长**：
    - **Tokenizer训练**：20.36小时。
    - **上下文模型最终连续训练**：1.92小时。
    - **接缝先验头部训练**：约0.86小时，修复聚焦的连续训练额外0.57小时。
    - **总训练路径**：约23.14小时。
- **预处理成本**：用于构建规范图块和接缝图的离线预处理是主要开销，ShapeNet训练池约需6.27小时，Objaverse-LVIS评估集约需7.39小时。
- **推理速度**：在线物体解码非常快速，C2LT-3D的部件归属对象解码平均**33.25毫秒/对象**，显著快于BPT（约87.42秒/对象）。

### 5. 实验数量与充分性

实验设计**非常充分且客观**，涵盖了多层次、多维度的评估：

- **主基准实验**：在1,024个固定对象ID上，与2个基线（BPT, VQ-Patch）进行详细的**固定对象协议**对比，并提供了**配对bootstrap置信区间**和**逐对象胜率**分析，确保了统计显著性。
- **消融实验**：
    - **因果消融**：分别移除典型化、分区上下文和接缝先验，验证每个因子独立且分离地影响其目标故障模式（如形状-姿态分离、跨部件泄漏、附着排序）。
    - **实现公平性控制**：为纯空间模型VQ-Patch授予“预言机（oracle）”般的部件归属权，与C2LT-3D原生暴露归属权进行对比，证明状态变量原生化的重要性。
    - **确定性实现敏感性扫描**：分析部件归属过滤器中不同阈值的影响。
- **能力与鲁棒性分析**：
    - 提供了**高复杂度切片**（≥10个部件）和**5,000对象全量粗糙基准**的结果。
    - 进行了**未过滤Objaverse-LVIS鲁棒性检查**和**分区噪声鲁棒性分析**。
    - 完成了**释放接口能力差距分析**，明确区分“输出合理几何”与“暴露可操作状态”。
    - 对**显式网格Token实现解码器**（一种三角条带生成器）进行了独立评估，验证状态到网格的还原能力。
- 实验评估**极其严格和透明**，指标、协议、预处理细节和范围限制文档详尽，有效控制了混杂变量，结论客观。

### 6. 论文的主要结论与发现

- **范式有效性**：以界面为中心的生成状态能够更鲁棒地迁移到开放世界多组件资产，在几何保真度和结构分离性上均优于压缩中心方法。
- **操作状态可执行性**：C2LT-3D暴露的潜在变量（特别是接缝变量）在对抗性附着设置下仍然**可操作**，能够成功完成潜在结构修复、干预和受控解码，而这是压缩中心标记器无法原生提供的。
- **因子化机制的分离性**：局部典型化、分区上下文和接缝先验各自针对特定的结构故障模式，其作用机制是**解耦的**，而非模型容量提升带来的统一增益。
- **评估标准转变**：论文主张，对于结构化3D生成，表征应由其**离散状态是否可用于装配级结构推理**来评判，而不仅仅是重建保真度。

### 7. 优点

- **概念创新性强**：提出了从“压缩码”到“操作状态”的清晰范式转变，将3D tokenizer的设计提升到了界面和操作层面。
- **方法设计精巧且透明**：C2LT-3D的三大因子化设计分别对应特定的解耦目标，且有明确的模块化架构和确定性操作，尤其是确定性的部件归属对象实现，避免了学到的后处理模块。
- **实验评估极其严谨和全面**：
    - 零样本跨数据集迁移，直接考验泛化能力。
    - 构建了专门的“致盲”修复任务（硬/启发式失败子集），巧妙验证了模型在局部几何误导下的推理能力，是方法有效性的强证据。
    - 细致的消融和公平性控制实验清晰地证明了各模块的独立贡献。
    - 完整的证据路线图（Evidence Roadmap）和对局限性的坦诚说明。
- **效率优势**：在实现更好结构的同时，推理速度远快于参数量巨大的压缩中心基线（BPT）。

### 8. 不足与局限

- **局部与全局的鸿沟**：论文明确指出，接缝兼容性是一种局部关系约束，并不提供形式化的全局拓扑或环路闭合保证。对于存在结构环（如车轮-车轴-底盘）的物体，局部有效性不足以保证全局一致性。
- **确定性实现的局限性**：部件归属对象实现依赖于预定义的无监督分区和无参数滤波器，其效果受限于分区质量。
- **前处理成本**：构建规范图块和接缝图的离线预处理计算成本较高，是一个不可忽视的系统负担。
- **生成范围**：论文的核心贡献在于表征的设计和验证，而非无条件生成的最新水平（SOTA）。受控序列化实验使用的是小型解码器，目的是状态实用性分析；显式网格实现也是教师强制下的能力测试。
- **应用限制**：下游应用需注意生成模型的共性风险，如可能导致误导性内容或未经授权的衍生资产，并且输出的结构有效性不等同于物理有效性证明。

（完）
