---
title: "ISAP-3D: Identity-Slot Aligned Part-Aware 3D Generation"
title_zh: "ISAP-3D: 身份-槽对齐的部件感知三维生成"
authors: "Junlin Hao, Haoshuai Fu, Xibin Song, Wei Li, Ruigang Yang, Xinggong Zhang, Jinchuan Zhang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12099v1"
tags: ["query:part-aware"]
score: 10.0
evidence: 通过身份-槽对齐解决槽置换导致的结构模糊，实现稳定的零件感知3D生成
tldr: 部分感知3D生成常因身份-布局纠缠产生结构歧义，现有方法或隐式推理导致部件分配不稳定，或依赖强布局先验难以实用。本文指出根源在于身份-槽排列自由：缺乏显式对齐时，部件与生成槽的对应关系不可识别，导致解耦不一致。为此提出ISAP-3D，用语义身份令牌锚定部件，进行身份条件的一对一布局预测，并以此指导几何合成，通过局部-全局条件保持全阶段身份对齐。此外构建了统一语义协议的部分级数据集，支持可学习对齐。实验证明，该方法显著提升结构稳定性、可控性和鲁棒性，超越现有基线。该工作揭示了身份-槽对齐对部分感知生成的核心作用，为结构化3D内容创建提供了可靠方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12099v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1179, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1266, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1235, \"height\": 287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1225, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1146, \"height\": 697, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1123, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1133, \"height\": 372, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12099v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1121, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12099v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 875, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12099v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 717, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有部分感知3D生成因身份-布局纠缠导致结构歧义，部件分配不稳定，且强布局条件难以获取，亟需解决身份-槽排列自由问题。
method: 提出ISAP-3D框架，以语义身份令牌实现一对一的身份-槽对齐，进行身份条件布局预测与几何合成，并构建统一语义数据集。
result: 实验表明ISAP-3D在结构稳定性、可控性和鲁棒性上显著优于现有部分感知生成方法，有效消除部件交换与合并现象。
conclusion: 身份-槽对齐是稳定部分感知生成的关键，ISAP-3D为此提供了端到端方案，推动了可解释的结构化3D内容生成。
---

## 摘要
部件感知的三维生成旨在合成具有语义意义组件的结构化对象，但常常由于身份-布局纠缠而导致结构模糊。现有方法要么隐式地推断部件身份和空间布局，这可能导致不稳定的部件分配（例如槽交换或部件合并），要么依赖于实践中难以获得的强布局条件。我们将这种模糊性归因于身份-槽排列自由度：如果没有明确的身份-槽对齐，语义部件与生成槽之间的对应关系在训练过程中不可识别，允许多个槽分配适应相同的监督信号，从而导致不一致的分解。基于这一见解，我们认为稳定的部件感知生成需要身份对齐的一对一槽建模。因此，我们提出了一个身份-槽对齐框架ISAP-3D，它用语义身份标记锚定每个部件，并执行以身份为条件的一对一布局预测，然后进行以布局为条件的几何合成。结构化的局部-全局条件化在语义、空间和几何阶段保持身份对齐。我们还构建了一个具有统一语义协议的部件级数据集，以实现可学习且一致的身份-槽对齐。大量实验表明，与最先进的部件感知生成基线相比，稳定性、可控性和鲁棒性都有所提高。

## Abstract
Part-aware 3D generation aims to synthesize structured objects with semantically meaningful components, yet often suffers from structural ambiguity due to identity-layout entanglement. Existing methods either infer part identity and spatial layout implicitly, which can lead to unstable part allocation (e.g., slot swapping or part merging), or rely on strong layout conditions that are difficult to obtain in practice. We attribute this ambiguity to identity-slot permutation freedom: without explicit identity-slot alignment, the correspondence between semantic parts and generation slots is not identifiable during training, allowing multiple slot assignments to fit the same supervision and leading to inconsistent decomposition. Based on this insight, we argue that stable part-aware generation requires identity-aligned one-to-one slot modelling. We therefore propose an identity-slot aligned framework, ISAP-3D, which anchors each part with semantic identity tokens and performs identity-conditioned one-to-one layout prediction, followed by layout-conditioned geometry synthesis. Structured local-global conditioning maintains identity alignment across semantic, spatial, and geometric stages. We also construct a part-level dataset with a unified semantic protocol to enable learnable and consistent identity-slot alignment. Extensive experiments demonstrate improved structural stability, controllability, and robustness over state-of-the-art part-aware generation baselines.

---

## 论文详细总结（自动生成）

好的，以下是对您提供的论文《ISAP-3D: Identity-Slot Aligned Part-Aware 3D Generation》的结构化深入总结。

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**：部件感知三维生成旨在将物体合成为具有语义意义的组件集合（如组装一个带机翼、机身、轮子的飞机），这对于三维内容的编辑、重用和交互式操作至关重要。
*   **核心问题**：现有方法普遍存在**结构不稳定性**，具体表现为生成的部件分配混乱（身份-槽排列自由），例如，在多次生成中，语义为“左臂”的部件可能出现在不同的生成槽中，或与其他部件合并、交换。这导致输出结果不一致、不可控。
*   **问题根源**：作者将此问题归因于**身份-布局纠缠**。传统方法要么隐式建模身份和布局，要么依赖固定的空间排序（如从左到右），均未在语义部件身份与生成槽之间建立显式、稳定的**一对一对应关系**。在训练中，多种槽分配方案可能都符合监督信号，使得这种对应关系无法被识别和固定下来。
*   **整体含义**：论文旨在解决这一根本性歧义，提出一个**身份-槽对齐**的生成框架，通过将语义身份显式地锚定在专属生成槽上，从而实现稳定、可控、结构一致的部件感知三维生成。

### 2. 论文提出的方法论

核心思想是建立一个“身份引导布局，布局引导几何”的二阶段生成流程，并在全流程中强制实施身份与生成槽的一对一对齐。

**关键技术细节与算法流程**：
1.  **问题形式化**：将部件感知生成定义为映射 `f : C → G`。输入条件`C`包括每个部件的文本描述、参考图像和可选的分割掩码，输出为一组与输入身份对齐的部件几何体`G`。中间引入粗体素表示`V`和部件包围盒`B`作为空间推理的桥梁：`C → (B, V) → G`。

2.  **身份感知的条件编码**：
    *   **局部身份条件 (`LCk`)**：对于每个部件`k`，其文本描述（如“头部”）被编码成语义令牌，同时利用掩码从输入图像中提取局部视觉特征。二者结合形成每个部件专属的身份条件。
    *   **全局上下文条件 (`GC`)**：将输入图像和粗体素`V`作为全局上下文令牌。此设计将部件级身份信号与物体级空间信号解耦。

3.  **以身份为条件的布局预测 (Identity-Conditioned Layout Prediction)**：
    *   **槽初始化与对齐**：为`K`个部件初始化`K`个并行的查询槽。通过一个Transformer架构，首先让每个槽通过**身份注意**交叉关注其对应的局部身份条件`LCk`，从而将语义身份注入并“锚定”到特定槽中。
    *   **槽交互与全局接地**：对齐后的槽通过**自注意机制**进行交互，学习部件间的空间关系；再通过**全局注意机制**关注全局上下文`GC`，确保其布局预测符合整体物体形状。
    *   **输出**：经过多层Transformer处理后，每个槽最终预测其对应部件的三维边界框`Bk`。

4.  **以布局为条件的几何合成 (Layout-Conditioned Geometry Synthesis)**：
    *   **布局条件生成**：使用预测的边界框`Bk`从粗体素`V`中裁剪出每个部件的局部体素区域。
    *   **身份一致性的几何推理**：通过一个“几何流Transformer”处理局部体素。该网络同样采用**局部注意**块来保持体素特征与部件身份条件的对齐，并用**全局注意**块实现跨部件信息交换和整体空间上下文融入。
    *   **输出**：最终通过解码器生成每个部件的精细三维几何形状。

5.  **数据集构建**：为支持身份对齐训练，作者基于PartVerseXL数据集构建了一个身份导向的数据集。其核心是将自由形式的文本描述替换为**封闭、统一的语义词汇表**，确保每个词符明确映射到一个部件（如使用“left hand”、“right hand”而非模糊的“hands”），并通过人工校验保证一致性。

### 3. 实验设计

*   **数据集**：作者自建的身份导向部件级数据集，包含约**8,000**个经过语义重新标注的物体，涵盖约**70**个统一的部件类别。100个用于评估，其余用于训练。
*   **评估指标**：
    *   **几何质量**：Chamfer Distance (CD) 和 F-Score (@0.1)。
    *   **结构连贯性**：部件间交互比，Part-wise Intersection over Union (Part-IoU)，越低表示几何分离越好。
    *   **身份-槽对齐度**：归一化互信息，Normalized Mutual Information (NMI)，衡量预测部件分配与真实分配之间的一致性。
*   **对比基准方法**：与四类代表性方法进行了比较。
    *   **PartField**：在整体生成网格上做后处理分割，无显式身份建模。
    *   **HoloPart**：基于分割结果生成部件，但分割本身不编码稳定的语义身份。
    *   **PartCrafter**：从图像直接预测，身份隐式出现。
    *   **OmniPart**：使用图像掩码作为身份条件，并采用自回归布局规划，但身份与槽未显式对齐。这是最强的基线。

### 4. 资源与算力

论文中**未明确提及**训练所使用的GPU型号、数量、批次大小或具体训练时长等算力资源信息。

### 5. 实验数量与充分性

实验设计较为丰富和全面，能够有效支撑其核心论点。
*   **主实验对比**：与4种代表性的基准方法在多个指标上进行定量和定性的全面比较。
*   **消融研究**：
    *   **训练条件消融**：分别移除文本、掩码和体素条件，分析不同模态对布局预测精度（Bbox IoU, Voxel IoU, Voxel Rec）的影响。
    *   **推理条件与粒度控制**：展示模型在仅用“文本+图像”、加入掩码、或提供真值布局等不同级别的条件下的生成鲁棒性和可控性。验证了通过修改文本描述即可控制部件分解的粒度。
*   **公平性**：对比的基线方法覆盖了不同技术流派，评估指标也兼顾了几何、结构和语义一致性，实验设计和对比是客观、公平的。

### 6. 论文的主要结论与发现

*   **身份-槽排列自由是核心障碍**：论文通过分析和实验证明，缺乏显式的身份-槽对齐是导致部件感知生成结构不稳定的根本原因。
*   **身份-槽对齐范式有效**：提出的ISAP-3D框架通过将语义身份显式锚定到专属槽，并在“身份→布局→几何”的全流程中保持对齐，能有效消除部件交换、合并等问题。
*   **性能显著提升**：实验结果表明，该方法在几何质量、结构连贯性和身份一致性方面均优于现有最先进方法，尤其是在衡量身份对齐的NMI指标上提升巨大。
*   **高可控性与鲁棒性**：该方法支持通过文本描述灵活控制部件分解粒度，且在不同强度的输入条件下（有/无掩码、布局）均表现鲁棒。

### 7. 优点

*   **问题定义深刻**：没有停留在现象描述，而是深入分析了结构模糊的本质——“身份-槽排列自由”，见解独到。
*   **方法设计严谨**：提出的框架通过显式的“身份槽”作为锚点，并设计与之配套的条件编码和网络架构，逻辑清晰，架构完整。
*   **结构化条件设计**：“局部身份条件”与“全局上下文条件”的分离是保障身份对齐的关键设计，有效实现了语义与空间的解耦。
*   **数据集贡献**：构建的具有统一语义协议的数据集是可复现研究的基础，为模型训练提供了稳定、明确的身份监督，具有独立贡献价值。
*   **实验充分且可视化透彻**：通过定性（视角一致性、部件颜色标记）和定量（NMI等新颖指标）实验，有力地论证了方法在身份对齐上的优越性。

### 8. 不足与局限

*   **算力成本未知**：论文未提供算力详情，无法评估其计算成本和可复现性门槛。
*   **数据规模与多样性有限**：自建数据集约8K物体、70个部件类别，相对于通用生成模型所用的数十亿量级数据集规模较小，可能限制了模型处理开放域世界中长尾物体或极端复杂部件组合的泛化能力。
*   **对底层表示的依赖**：布局预测依赖粗体素表示，几何合成也继承自基线模型的架构（基于流的Transformer），整个框架的性能上限受到这些底层表示能力的约束。
*   **未涉及纹理生成**：当前工作仅关注部件几何结构的生成，未涉及色彩、材质等外观属性的生成，离最终可直接编辑的三维资产仍有一定距离。
*   **类别固定性**：统一的语义词汇表虽然带来了稳定性，但也固化了模型可以生成的部件类别，缺乏像大语言模型那样从自由文本中理解新概念并生成“开放词汇”部件的能力。

（完）
